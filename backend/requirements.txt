# ──────────────────────────────────────────────────────────────────────
# ShelfOps API — Requirements (NO ML dependencies)
# ML deps live in requirements-ml.txt (separate container)
# ──────────────────────────────────────────────────────────────────────

# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Redis
redis[hiredis]>=5.0.0

# Background Tasks
celery[redis]>=5.3.0

# HTTP Client
httpx>=0.26.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
authlib>=1.3.0

# Integrations (SMB — REST API)
tenacity>=8.2.0
cryptography>=41.0.0

# Enterprise Integrations
asyncssh>=2.14.0            # SFTP batch file transport
aiokafka>=0.10.0            # Kafka event streaming consumer
kaggle>=1.6.0               # Kaggle dataset downloads

# Email
sendgrid>=6.11.0

# Observability
structlog>=24.1.0
PyYAML>=6.0.1

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
factory-boy>=3.3.0

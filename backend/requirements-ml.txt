# ──────────────────────────────────────────────────────────────────────
# ShelfOps ML Worker — Requirements
# Runs in Dockerfile.ml (separate container from API)
# ──────────────────────────────────────────────────────────────────────

# Core ML
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost>=2.0.0
tensorflow>=2.15.0
joblib>=1.3.0

# Experiment Tracking
mlflow>=2.10.0

# Model Explainability
shap>=0.44.0

# Data Validation
pandera>=0.18.0

# Visualization (analysis + SHAP plots)
plotly>=5.18.0
seaborn>=0.13.0
matplotlib>=3.8.0
kaleido>=0.2.1              # Static image export from Plotly

# Shared with API (needed by ML code)
celery[redis]>=5.3.0
redis[hiredis]>=5.0.0
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
structlog>=24.1.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
PyYAML>=6.0.1
